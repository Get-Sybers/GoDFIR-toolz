*.zip
__pycache__/

# build-all.sh's per-checkout controller venv (requirements.txt)
.venv/
# ansible-lint / ansible-galaxy local caches; build-all.sh's in-tree
# community.docker install (requirements.yml)
.ansible/
