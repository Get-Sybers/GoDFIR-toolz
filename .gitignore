*.zip
__pycache__/

# build-all.sh's per-checkout controller venv (the pins live in the script)
.venv/
# ansible-lint / ansible-galaxy local caches; build-all.sh's in-tree
# community.docker install
.ansible/
