# GoDFIR-toolz ansible defaults — read from the repo root (build-all.sh cd's
# here before launching the playbook), so the collection's role resolves from
# the source checkout without installing anything.
[defaults]
roles_path = roles
